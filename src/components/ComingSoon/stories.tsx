import { Story, Meta } from '@storybook/react'
import ComingSoon from '.'

export default {
  title: 'ComingSoon',
  component: ComingSoon
} as Meta

export const Default: Story = () => <ComingSoon />
